# Example environment variables for this project
# Fill these in (do NOT commit secrets to a public repo).

# Django / general
SECRET_KEY=

# Database (Postgres)
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# Email
EMAIL_BACKEND=
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USE_TLS=
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Intra (42) OAuth
CLIENT_ID=
CLIENT_SECRET=
REDIRECT_URI=
OUUTH_TOKEN_URI=
# AUTH_URI (optional)
AUTH_URI=
FRONTEND_REDIRECT_URL=

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGOLE_TOKEN_URI=

# Notes:
# - Some variable names above match those used in the code (including two misspellings: "OUUTH_TOKEN_URI" and "GOOGOLE_TOKEN_URI").
#   Keep those names if you rely on the current code, or fix the typos in code and update this file accordingly.
# - The project also uses python-dotenv/environ to load environment variables from a .env file.
# - After populating this file, copy it to ".env" (or set real environment variables) for local development.
